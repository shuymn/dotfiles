# Hunk is the review-first diff surface for human/agent changesets.
# Keep global defaults conservative; use Lazygit custom commands for watch mode.

theme = "catppuccin-mocha"
mode = "auto"
vcs = "git"

watch = false
exclude_untracked = false
line_numbers = true
wrap_lines = false
agent_notes = true
transparent_background = true
